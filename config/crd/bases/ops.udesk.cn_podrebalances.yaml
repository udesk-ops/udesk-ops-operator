---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: podrebalances.ops.udesk.cn
spec:
  group: ops.udesk.cn
  names:
    kind: PodRebalance
    listKind: PodRebalanceList
    plural: podrebalances
    shortNames:
    - pr
    singular: podrebalance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .spec.autoApproval
      name: Auto-Approval
      type: boolean
    - jsonPath: .spec.strategy.type
      name: Strategy
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: PodRebalance is the Schema for the podrebalances API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PodRebalanceSpec defines the desired state of PodRebalance.
            properties:
              autoApproval:
                default: false
                description: AutoApproval enables automatic approval for rebalancing
                type: boolean
              dryRun:
                default: false
                description: DryRun mode for testing rebalancing without actual execution
                type: boolean
              namespace:
                description: Namespace where to perform rebalancing
                type: string
              notificationType:
                description: NotificationType defines notification method
                enum:
                - Email
                - WXWorkRobot
                type: string
              notifyMsgTemplate:
                description: NotifyMsgTemplate is the reference to notification template
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              selector:
                description: Selector for target pods
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              strategy:
                description: RebalanceStrategy defines how pods should be rebalanced
                properties:
                  parameters:
                    additionalProperties:
                      type: string
                    description: Parameters for the strategy
                    type: object
                  threshold:
                    description: Threshold configuration for triggering rebalancing
                    properties:
                      cpu:
                        description: CPU usage threshold percentage
                        format: int32
                        maximum: 100
                        minimum: 0
                        type: integer
                      memory:
                        description: Memory usage threshold percentage
                        format: int32
                        maximum: 100
                        minimum: 0
                        type: integer
                      podCountImbalance:
                        description: Pod count imbalance threshold
                        format: int32
                        minimum: 1
                        type: integer
                    type: object
                  type:
                    description: Type of rebalancing strategy (NodeBalance, ResourceBalance,
                      etc.)
                    enum:
                    - NodeBalance
                    - ResourceBalance
                    - AntiAffinity
                    type: string
                required:
                - type
                type: object
              timeout:
                default: 30m
                description: Timeout for the approval and rebalancing process
                pattern: ^(\d+)([smhdw])$
                type: string
            required:
            - namespace
            - selector
            - strategy
            type: object
          status:
            description: PodRebalanceStatus defines the observed state of PodRebalance.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              message:
                description: Message provides additional information about the current
                  status
                type: string
              rebalanceBeginTime:
                description: RebalanceBeginTime records when rebalancing started
                format: date-time
                type: string
              rebalanceEndTime:
                description: RebalanceEndTime records when rebalancing completed
                format: date-time
                type: string
              rebalancedPods:
                description: RebalancedPods contains information about pods that were
                  rebalanced
                items:
                  description: RebalancedPodInfo contains information about a rebalanced
                    pod
                  properties:
                    name:
                      description: Name of the pod
                      type: string
                    namespace:
                      description: Namespace of the pod
                      type: string
                    sourceNode:
                      description: Source node
                      type: string
                    status:
                      description: Rebalance status for this pod
                      enum:
                      - Pending
                      - Moving
                      - Completed
                      - Failed
                      type: string
                    targetNode:
                      description: Target node
                      type: string
                    timestamp:
                      description: Timestamp when this pod was rebalanced
                      format: date-time
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
              status:
                description: Status of the rebalancing process
                enum:
                - Pending
                - Approvaling
                - Approved
                - Rejected
                - Executing
                - Completed
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
