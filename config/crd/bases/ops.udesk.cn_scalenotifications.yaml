---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: scalenotifications.ops.udesk.cn
spec:
  group: ops.udesk.cn
  names:
    kind: ScaleNotification
    listKind: ScaleNotificationList
    plural: scalenotifications
    singular: scalenotification
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.default
      name: Default
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ScaleNotification is the Schema for the scalenotifications API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScaleNotificationSpec defines the desired state of ScaleNotification.
            properties:
              config:
                description: |-
                  Config contains the specific configuration for the notification.
                  It uses runtime.RawExtension to support polymorphism.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              default:
                default: false
                description: default indicates if this notification is the default
                  for the scale operation.
                type: boolean
              type:
                description: Type is the type of notification (e.g., email, wxworkrobot).
                enum:
                - email
                - wxworkrobot
                type: string
            required:
            - type
            type: object
          status:
            description: ScaleNotificationStatus defines the observed state of ScaleNotification.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
